<%= form_with(model: game_history) do |form| %>
  <% if game_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_history.errors.count, "error") %> prohibited this game_history from being saved:</h2>

      <ul>
        <% game_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.text_field :game_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :date_played %>
    <%= form.datetime_select :date_played %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
